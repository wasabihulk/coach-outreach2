# Coach Outreach System v8.0 - Dependencies
# ============================================================================

# Core dependencies
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Gmail API (for Railway - SMTP blocked)
google-api-python-client>=2.100.0
google-auth>=2.22.0

# Web framework
flask>=2.3.0
flask-cors>=4.0.0
gunicorn>=21.0.0  # Production server for Railway/Render

# Utilities
requests>=2.31.0
python-dotenv>=1.0.0  # Load .env files for local development
schedule>=1.2.0  # For email scheduling

# Database (Supabase)
supabase>=2.0.0

# Encryption (for per-athlete Gmail credentials)
cryptography>=41.0.0
